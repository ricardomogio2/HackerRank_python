#Python 3.7 version - HackerRank solution for "Arithmetic Operators"

if __name__ == '__main__':
    a = int(input())
    b = int(input())


    print(a+b) #Printing the sum
    print(a-b) #Printing the difference 
    print(a*b) #Printing the product
